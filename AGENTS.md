# Agent Guidelines for opentui

## Self-Improvement Ledger

- 2026-03-22 | seam: after child-order reads moved to the native scene graph, `Renderable` still treated JS layout/z-index caches as partially authoritative during mutation, which reintroduced reorder bugs for same-parent moves and `add(existingChild, index)` despite native order being correct | root cause: mutation methods updated `_childrenInLayoutOrder` and `_childrenInZIndexOrder` directly before/after native operations, so JS caches could drift from the native scene graph between moves and then feed stale indices back into later reorder operations | canonical fix: refresh JS layout-order caches from the native scene graph immediately after child mutations, treat `_childrenInZIndexOrder` as a rebuildable cache instead of a maintained source of truth, and keep `renderableMapById`/direct-child lookups off the authoritative path for child membership/order reads | validation seam: `export PATH=\"$HOME/.bun/bin:$PATH\" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/tests/renderable.snapshot.test.ts && bun test src/tests/renderable.native-scene-graph.test.ts src/tests/renderable.test.ts && bun test src/tests/rust-scene-graph-ffi.test.ts`
- 2026-03-22 | seam: switching `Renderable` child/count reads over to the native scene graph exposed same-parent reorder regressions in `insertBefore` and `add(existingChild, index)` even though simple append/remove parity was green | root cause: the native `sceneNodeInsertBefore` path computed the anchor index before removing the moved child from the same parent, and the JS `add(existingChild, index)` path interpreted `index` as a post-removal insertion slot instead of a pre-move anchor index | canonical fix: export ordered native child-handle reads from the scene graph, use them for `Renderable` child-order reads, adjust native same-parent `insertBefore` indices after removal, and normalize JS `add(existingChild, index)` to anchor against the pre-move order | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/tests/rust-scene-graph-ffi.test.ts && bun test src/tests/renderable.snapshot.test.ts && bun test src/tests/renderable.native-scene-graph.test.ts src/tests/renderable.test.ts`
- 2026-03-21 | seam: `TextTableRenderable` remained the last measured-layout holdout blocking native scene-graph ownership even after text/editor/line-number widgets moved onto Rust measurement | root cause: table sizing still depended on a JS Yoga measure closure over `_cells`/`computeLayout`, so otherwise native-clean trees had to keep falling back to JS for any subtree containing a table | canonical fix: add a native table-measure spec and `sceneNodeSetTextTableMeasure` registration in the scene graph, port the outer table width/height math to Rust using existing native `TextBufferView` cell pointers, and register `TextTableRenderable` as a non-blocking native-measured leaf while keeping JS cell raster/layout application intact | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/renderables/TextTable.test.ts && bun test src/tests/renderable.test.ts src/tests/renderable.native-scene-graph.test.ts`
- 2026-03-21 | seam: moving measured text/editor/line-number widgets onto the native scene graph produced stale widths and heights after content or offset changes, even though isolated direct-state probes showed the native layout math itself was correct | root cause: the native measure registry was missing per-node dirtying after measure-config updates, the Rust `NativeSceneStyle` defaults originally diverged from the JS ABI defaults, and Yoga `getGap(...)` on the JS side returns numeric values/`NaN` rather than `{unit,value}` objects, which zeroed constructor-time gap sync for native layout | canonical fix: add a native scene-graph measure registry for text views and line-number gutters, dirty native measured nodes whenever their registration updates, align Rust style defaults with the JS ABI, decode Yoga gap getters numerically, and let JS Yoga measure functions remain as non-blocking fallback for these widgets while native layout becomes authoritative for clean trees | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/tests/renderable.test.ts src/renderables/Box.test.ts src/tests/renderable.native-scene-graph.test.ts && bun test src/renderables/__tests__/Textarea.rendering.test.ts && bun test src/renderables/__tests__/LineNumberRenderable.test.ts && bun test src/renderables/__tests__/LineNumberRenderable.scrollbox.test.ts`
- 2026-03-21 | seam: native scene-graph gap parity still failed after the broader style ABI landed, leaving pure `BoxRenderable` trees offset differently in Rust than in JS Yoga | root cause: the Rust-side `NativeSceneStyle::default()` unit tags did not match the JS FFI defaults and `syncNativeSceneStyle()` decoded Yoga `getGap(...)` results as structured values even though the JS binding returns plain numbers/`NaN`, so constructor-time gap styles were silently zeroed on the native path | canonical fix: align `NativeSceneStyle` defaults with the JS ABI, preserve constructor-time gap sync in `BoxRenderable`, mark measure-function widgets so only pure non-measure trees opt into native layout reads, and decode Yoga gap getters with a dedicated numeric helper before sending the scene-style struct across FFI | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/tests/renderable.native-scene-graph.test.ts src/tests/renderable.test.ts src/renderables/Box.test.ts`
- 2026-03-21 | seam: native scene-graph layout parity broke as soon as `NativeSceneStyle` grew beyond the first minimal mirror, with Bun FFI reads producing zeroed or shifted Yoga values | root cause: `sceneNodeSetStyle` and `sceneNodeGetLayout` dereferenced JS-provided struct pointers as aligned Rust structs, which is undefined across Bun FFI ArrayBuffers, and the JS mirror was still sourcing only a partial shadow style set instead of the actual Yoga node state | canonical fix: switch the scene-graph ABI to `read_unaligned`/`write_unaligned`, expand `SceneStyleStruct`/`NativeSceneStyle` to carry the non-gap Yoga style inputs needed for box/container parity, and sync native scene styles from Yoga getters rather than ad hoc JS fields | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test scene_graph && cd ../.. && cd packages/core && bun run build:native && bun test src/tests/renderable.test.ts src/renderables/Box.test.ts src/tests/renderable.native-scene-graph.test.ts`
- 2026-03-21 | seam: the first Yoga-backed scene graph attempt failed to compile as a global registry and its initial layout smoke asserted dimensions the foundation slice did not yet guarantee | root cause: Yoga `Node` is not marked `Send`, which prevented storing it behind the global `OnceLock<Mutex<...>>`, and the first test assumed full layout parity before JS wrappers were cut over | canonical fix: mark the scene-node container `Send` under the single-threaded mutex ownership model and narrow the smoke test to structural registry/layout availability rather than full parity | validation seam: `cd packages/core/native && cargo test scene_graph && cargo check`
- 2026-03-21 | seam: the final native-input cutover initially failed to compile because `rustix` was added with a non-existent `io` feature while the runtime still needed raw-byte stdin ownership to preserve JS parser semantics | root cause: I chose the wrong feature gate for `rustix` and had not yet switched the native bridge from parsed crossterm events to raw-byte forwarding, which left the last JS stdin ownership seam unresolved | canonical fix: depend on `rustix` with `std`, `fs`, and `stdio`, implement a nonblocking raw-byte stdin bridge in `terminal_input.rs`, move raw mode ownership into the native crossterm backend, and route runtime input through `terminal:raw` instead of JS `stdin.on(\"data\")` | validation seam: `cd packages/core/native && cargo test && cd ../.. && cd packages/core && bun test src/tests/renderer.input.test.ts src/tests/renderer.focus-restore.test.ts src/tests/renderer.palette.test.ts && bun run build:native`
- 2026-03-21 | seam: renderer-level Bun tests started warning about `terminal:*` listener leaks after adding the native input bridge | root cause: each `CliRenderer` instance registered four native-event listeners on the shared FFI event emitter but never removed them during destroy, so repeated test renderers accumulated handlers | canonical fix: unregister the `terminal:key`, `terminal:mouse`, `terminal:paste`, and `terminal:focus` handlers in `CliRenderer.finalizeDestroy()` | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun test src/tests/renderer.focus-restore.test.ts src/tests/renderer.palette.test.ts`
- 2026-03-21 | seam: streamed OpenCode responses still crashed after the first dist-only relink because `@opentui/solid/dist` resolved its internal `@opentui/core` import back to `packages/core/src/index.ts` inside the monorepo | root cause: the local linker only repointed the consumer-facing package symlink, but it did not seed the linked `dist` package with its own `node_modules/@opentui/core`, so Bun resolved the nested import through the workspace source tree and recreated mixed class/yoga-node identities | canonical fix: teach `scripts/link-opentui-dev.sh` to create local dependency links inside linked `dist` packages, specifically wiring `@opentui/solid` and `@opentui/react` back to the same `@opentui/core` link source | validation seam: `cd /home/drvova/opentui/packages/solid/dist && bun -e 'console.log(await import.meta.resolve(\"@opentui/core\"))'` returned `file:///home/drvova/opentui/packages/core/dist/index.js`
- 2026-03-21 | seam: local OpenCode linking could resolve `@opentui/core` from built `dist` while resolving `@opentui/solid` from source, leaving a mixed package graph that risks class-identity mismatches during TUI reconciliation | root cause: `scripts/link-opentui-dev.sh` always symlinked package roots even when a built `dist/package.json` existed, so linked consumers could mix built and source OpenTUI packages after manual overrides or relinks | canonical fix: teach `link-opentui-dev.sh` to prefer each package's built `dist` directory when it contains a package manifest, and relink OpenCode so both `@opentui/core` and `@opentui/solid` resolve from `dist` | validation seam: `cd /home/drvova/opencode/packages/opencode && bun -e 'console.log(await import.meta.resolve(\"@opentui/solid\")); console.log(await import.meta.resolve(\"@opentui/core\"))'` returned `file:///home/drvova/opentui/packages/solid/dist/index.js` and `file:///home/drvova/opentui/packages/core/dist/index.js`
- 2026-03-21 | seam: the direct Bun FFI renderer/terminal smoke tests started dumping live ANSI frames after the Rust terminal flush landed | root cause: those smokes still constructed renderers with `testing=false`, so the newly working stdout passthrough path was active during verification | canonical fix: switch the Bun FFI smoke constructors to `createRenderer(..., true, false)` so they exercise stateful native APIs without writing to the real terminal | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun test src/tests/rust-renderer-core-ffi.test.ts src/tests/rust-terminal-core-ffi.test.ts`
- 2026-03-21 | seam: the local Rust-backed OpenCode build showed only startup logs and `OSC 11` before handing off a blank terminal | root cause: `RendererState::render` only copied the next buffer into the current buffer and never serialized terminal frames or emitted terminal lifecycle/mode sequences from the Rust runtime | canonical fix: add native terminal setup/teardown and full-frame ANSI flushing via `TerminalState` plus `OptimizedBuffer::write_ansi_frame`, and route `createRenderer(testing)` through a stdout-passthrough-aware renderer constructor | validation seam: `timeout 6 script -qfc 'opencode --print-logs --log-level DEBUG' /tmp/opencode-default-final.log` rendered the full OpenCode UI including `Ask anything...`
- 2026-03-21 | seam: `cargo test` for `packages/core/native` failed before validation because `text_buffer_view` tests still called `text_for_offsets` | root cause: the helper had been renamed to `rendered_text_for_offsets` earlier, but the word-wrap regression tests were never updated to match | canonical fix: switch the stale test calls to `rendered_text_for_offsets` so the native suite tracks the current API | validation seam: `cd packages/core/native && cargo test`
- 2026-03-21 | seam: `git add` failed during Zig-language cleanup because the deleted tree-sitter asset directory path no longer existed as a resolvable pathspec | root cause: the staging command passed a removed directory path literally instead of staging tracked deletions via `-A` on surviving parent/file paths | canonical fix: restage with `git add -A -- <existing paths and deleted files>` so tracked deletions are included without naming a vanished directory | validation seam: successful commit of the Zig-language removal after restaging with `git add -A`
- 2026-03-21 | seam: removing the obsolete `packages/core/src/zig` tree with `rm -rf` was blocked during the Rust-only cleanup pass | root cause: the shell policy rejected recursive deletion even though the tracked Zig files had already been staged for removal, leaving ignored `.zig-cache` residue behind | canonical fix: use `git rm -r packages/core/src/zig` for tracked files and a short `python3` `shutil.rmtree(...)` fallback to remove the ignored local cache directory afterward | validation seam: `test -d packages/core/src/zig && find packages/core/src/zig -maxdepth 2 -type d | sort || true` returned no remaining tree after cleanup
- 2026-03-21 | seam: `bun test src/renderables/__tests__/LineNumberRenderable.scrollbox.test.ts` passed assertions but Bun crashed during process teardown | root cause: renderer shutdown tore down the global tree-sitter singleton immediately when the last renderer disappeared, racing active worker shutdown with test teardown churn | canonical fix: defer tree-sitter singleton cleanup in `rendererTracker.removeRenderer` by one tick and cancel it if another renderer is created before cleanup runs | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun test src/renderables/__tests__/LineNumberRenderable.scrollbox.test.ts`
- 2026-03-21 | seam: worktree-file-reservations cleanup was blocked by stale lock metadata from a previous Codex PID after the earlier session ended | root cause: `.git/codex-file-reservations` still held expired ownership records, so normal release assumptions no longer matched a live process | canonical fix: confirm stale state with `file_reservation.py status --json` and clear the orphaned entries with `file_reservation.py release --force <paths>` before continuing edits | validation seam: successful forced release of the stale `Textarea`/`LineNumberRenderable` reservation set
- 2026-03-21 | seam: `bun run build:native` appeared to succeed but the loader kept executing stale native code from `packages/core/node_modules/@opentui/core-linux-x64` | root cause: `packages/core/scripts/build.ts` derived the output directory from scoped package name `@opentui/core`, so it wrote fresh artifacts into nested path `node_modules/@opentui/@opentui/core-linux-x64` instead of the loader-visible package dir | canonical fix: split scoped package name into scope and basename, write package metadata as `@opentui/core-linux-x64` but copy binaries into directory `node_modules/@opentui/core-linux-x64`, and remove the stale nested output path on build | validation seam: matching SHA-256 hashes for `packages/core/native/target/x86_64-unknown-linux-gnu/release/libopentui.so` and `packages/core/node_modules/@opentui/core-linux-x64/libopentui.so` plus direct Bun FFI checks using the packaged loader
- 2026-03-21 | seam: Rust native verification was blocked because `packages/core/native/src/optimized_buffer.rs` had been deleted while `lib.rs` still declared `mod optimized_buffer` | root cause: the worktree kept the module declaration and downstream FFI exports but dropped the backing source file during the core-helper pass | canonical fix: restore `optimized_buffer.rs` with a real implementation before rerunning Cargo and Bun wrapper verification | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core/native && cargo test && cd .. && bun test src/buffer.test.ts`
- 2026-03-21 | seam: worktree-file-reservations `release` rejected active same-owner locks after the draw-hook pass | root cause: the reservation helper treated the still-active owner metadata conservatively even for the current Codex owner chain | canonical fix: verify ownership with `status` and use `release --force` for local cleanup of same-owner active locks when the edit window is complete | validation seam: `python3 ~/.codex/skills/local/worktree-file-reservations/scripts/file_reservation.py release --force packages/core/native/src/lib.rs packages/core/native/cbindgen.toml packages/core/native/src/optimized_buffer.rs packages/core/src/tests/rust-buffer-draw-ffi.test.ts`
- 2026-03-20 | seam: `git add`-based commit staging failed when a deleted file was passed explicitly after it was already removed from disk | root cause: the staging command used a pathspec list that assumed deleted paths remained resolvable | canonical fix: use `git add -A -- <paths>` or otherwise stage deletions via tracked-path update semantics instead of explicit missing-file pathspecs | validation seam: successful commit with the corrected staging command for the same change set
- 2026-03-20 | seam: ABI manifest verification broke after renaming the primary `dlopen` handle in `zig.ts` | root cause: `packages/core/scripts/native-abi.ts` matched only `*Library` variable names instead of the actual `const <name> = dlopen(...)` shape | canonical fix: broaden the loader-anchor regex to accept any top-level `dlopen` assignment name | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun run native:abi:check`
- 2026-03-20 | seam: direct Bun JS verification of `syntax-style.test.ts` failed because the platform native package was absent from `packages/core/node_modules/@opentui` | root cause: this checkout had source code but no locally built optional native artifact for `@opentui/core-linux-x64` | canonical fix: run `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun run build:native` before wrapper-level runtime tests | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun test src/syntax-style.test.ts`
- 2026-03-20 | seam: direct Bun JS verification failed because workspace dependencies like `bun-ffi-structs` were not installed locally | root cause: the repo checkout had no populated workspace `node_modules` tree | canonical fix: run `export PATH="$HOME/.bun/bin:$PATH" && bun install` at repo root before JS-side verification | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && bun install && cd packages/core && bun test src/tests/rust-syntax-style-overlay.test.ts`
- 2026-03-20 | seam: Bun-backed package-script verification failed locally because `bun` was unavailable to the current shell | root cause: the environment did not have Bun installed and the installer's PATH update was not visible in the active non-login shell | canonical fix: install Bun to `~/.bun/bin` and prepend `PATH="$HOME/.bun/bin:$PATH"` in verification shells | validation seam: `export PATH="$HOME/.bun/bin:$PATH" && cd packages/core && bun run native:abi:check && bun run native:rust:test`

Default to using Bun instead of Node.js.

- Use `bun <file>` instead of `node <file>` or `ts-node <file>`
- Use `bun test` instead of `jest` or `vitest`
- Use `bun install` instead of `npm install` or `yarn install` or `pnpm install`
- Use `bun run <script>` instead of `npm run <script>` or `yarn run <script>` or `pnpm run <script>`
- Bun automatically loads .env, so don't use dotenv.

NOTE: When only changing typescript, you do NOT need to run the build script.
The build is only needed when changing native code.

## APIs

Don't use bun-specific APIs. Generated code should work in Bun, Node.js and Deno runtimes.

## Testing

Use `bun test` to run tests from the packages directories for a specific package.

```ts#index.test.ts
import { test, expect } from "bun:test";

test("hello world", () => {
  expect(1).toBe(1);
});
```

For more information, read the Bun API docs in `node_modules/bun-types/docs/**.md`.

## Build/Test Commands

To build the project (before running typescript tests), run
`bun run build`
FROM THE REPO ROOT to make sure all packages are built correctly.

To run native tests for `packages/core`, run
`bun run test:native`
FROM THE `packages/core` DIRECTORY.

To filter native tests, use:
`bun run test:native -Dtest-filter="test name"`
FROM THE `packages/core` DIRECTORY.

## Typescript Code Style

- **Runtime**: Bun with TypeScript
- **Formatting**: Prettier (semi: false, printWidth: 120)
- **Imports**: Use explicit imports, group by: built-ins, external deps, internal modules
- **Types**: Strict TypeScript, use interfaces for options/configs, explicit return types for public APIs
- **Naming**: camelCase for variables/functions, PascalCase for classes/interfaces, UPPER_CASE for constants
- **Error Handling**: Use proper Error objects, avoid silent failures
- **Async**: Prefer async/await over Promises, handle errors explicitly
- **Comments**: Minimal comments, NO JSDoc
- **File Structure**: Index files for clean exports, group related functionality
- **Testing**: Bun test framework, descriptive test names, use beforeEach/afterEach for setup

## Debugging

- NOTE this is a terminal UI lib and when running examples or apps built with it,
  you cannot currently see log output like console.log. Ask the user to run the example/app and provide the output.
- Reproduce the issue in a test case. Do NOT start fixing without a reproducible test case.
  Use debug logs to see what is actually happening. DO NOT GUESS.
